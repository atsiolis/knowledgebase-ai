# Use Nginx to serve static files
FROM nginx:alpine

# Copy only the frontend static files to Nginx's default public directory
COPY frontend/ /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# Nginx starts automatically